// constants/constants.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color softBlue =
      Color(0xFF89CFF0); // Soft Blue for a calming effect
  static const Color paleYellow =
      Color(0xFFFFF8E1); // Pale Yellow for a light and subtle background
  static const Color peach = Color(0xFFFFD1B2); // Peach as a warm accent color
  static const Color coral =
      Color(0xFFFF6F61); // Coral for buttons or interactive elements
  static const Color deepNavy =
      Color(0xFF002D72); // Deep Navy for dark, contrasting elements
}

class AppDimensions {
  static const double padding = 16.0;
  static const double cornerRadius = 8.0;
  static const double buttonHeight = 48.0;
}
